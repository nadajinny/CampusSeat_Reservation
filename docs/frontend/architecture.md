# 🧩 프런트엔드 아키텍처 및 폴더 구조 (Static HTML + JS)

이 프로젝트의 프런트엔드는 **번들러 없이 동작하는 정적 HTML/CSS/JS 구조**입니다.  
화면은 HTML 파일로 분리되어 있고, 공통 로직은 `frontend/js/`에 모여 있습니다.

---

## 1. 폴더 구조

Plaintext

`frontend/
 ├── index.html
 ├── login.html
 ├── dashboard.html
 ├── search-availability.html
 ├── meeting-room-reservation.html
 ├── seat-reservation.html
 ├── my-reservations.html
 ├── css/
 │   └── style.css
 ├── js/
 │   ├── app.js
 │   ├── api-client.js
 │   ├── reservation-engine.js
 │   └── auth.js
 └── __tests__/
     ├── reservation-engine.test.js
     └── api-client.test.js`

---

## 2. 핵심 모듈 역할

### 2.1 `frontend/js/app.js` (UI 흐름 오케스트레이터)

- 예약 가능 시간 조회 → 예약 상세 → 예약 생성 흐름을 담당합니다.
- 페이지별 초기화 함수로 화면을 구분합니다.
  - `initSearchPage()`
  - `initMeetingReservationPage()`
  - `initSeatReservationPage()`
- 페이지 간 이동을 위해 `sessionStorage`의 `pendingReservation`을 사용합니다.
- **주의:** DOM ID가 바뀌면 동작이 깨질 수 있으므로 문서에 명시된 ID를 유지해야 합니다.

### 2.2 `frontend/js/api-client.js` (API 통신 레이어)

- `/api/*` 엔드포인트 호출을 통합합니다.
- `ApiError`를 통해 오류 메시지/코드를 표준화합니다.
- `accessToken`이 있으면 `Authorization: Bearer` 헤더를 자동 첨부합니다.

### 2.3 `frontend/js/reservation-engine.js` (정책 검증 로직)

- 회의실/좌석 예약 정책을 순수 JS 함수로 검증합니다.
- **UI와 분리된 순수 로직**이므로 Jest 테스트 대상으로 사용됩니다.
- 현재 UI에서는 직접 호출하지 않지만, 규칙 변경 시 여기와 테스트를 함께 수정합니다.

### 2.4 `frontend/js/auth.js` (레거시 로그인 스크립트)

- 별도 파일이 존재하지만 현재 `login.html`은 **inline script**로 로그인 흐름을 처리합니다.
- 필요 시 `login.html`을 리팩터링하여 `auth.js`로 이동할 수 있습니다.

---

## 3. 사용자 흐름 (Page Flow)

1. `index.html` → `login.html` 자동 이동  
2. 로그인 성공 → `dashboard.html`  
3. `search-availability.html`에서 시설 유형/날짜 선택  
4. 회의실 또는 좌석 예약 페이지로 이동  
5. 예약 성공 후 `my-reservations.html` 또는 다시 검색 화면으로 이동

---

## 4. 데이터 흐름 (요약)

- **로그인 결과**: `studentId`, `accessToken`이 `sessionStorage`에 저장됨
- **예약 진행 중 상태**: `pendingReservation`에 선택한 날짜/시간이 저장됨
- **예약 가능 시간**: `/api/status/*` 응답을 기반으로 시간/좌석 선택 UI 구성

