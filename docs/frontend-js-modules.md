# 🧠 JavaScript 모듈 & 테스트

프런트엔드의 핵심 로직은 세 개의 모듈로 나뉘어 있으며, ReservationEngine은 Jest로 검증합니다.

| 파일 | 책임 |
| --- | --- |
| `js/api-client.js` | `fetch` 래퍼. 토큰 자동 첨부, 공통 에러 처리(`ApiError`). 로그인/현황조회/예약생성/취소 등 모든 HTTP 호출을 함수로 제공. |
| `js/app.js` | 싱글톤 `state`로 사용자 ID, 선택 조건, API 응답을 저장. 화면별 초기화 함수(`initSearchPage`, `initMeetingReservationPage`, …)를 DOMContentLoaded에서 호출. 예약 진행 정보는 `pendingReservation` 키로 `sessionStorage`에도 복제. |
| `js/reservation-engine.js` | 프런트에서 실행되는 검증기. 운영시간(09~18), 회의실 최소 3명, 주·일 제한, 중복 좌석/시간 등 모든 비즈니스 규칙을 캡슐화. UI는 `engine.validateMeeting/validateSeat` 결과의 `{ ok, code, message }`를 기반으로 사용자 피드백을 출력. |

## 단위 테스트

- `frontend/__tests__/reservation-engine.test.js`
  - Jest로 21개 케이스를 검증합니다.
  - `npm test` 실행 시 `reservation-engine.js`만 로드되므로 DOM 없이 빠르게 피드백을 받을 수 있습니다.
